const filhas = ['Ana', 'Maria']
const filhos = ['João', 'Pedro']
// Concatenando os dois arrays
const todos = filhas.concat(filhos)

// Concatenando todos e adicionando mais um
const todosMaisUm = filhas.concat(filhos, 'Fulano')
console.log(todos, todosMaisUm)